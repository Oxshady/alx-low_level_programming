#include "main.h"
/**
 * main - testing the positive or negative function in case 0
 * Return: zero if success
 */
int main(void)
{
	int i = 0;

	if (i == 0)
		positive_or_negative(i);
	return (0);
}
